{% extends "base_admin.html.twig" %}

{% block title %}
    BlogSymfony | Nouvelle catégorie
{% endblock %}

{% block body %}

<h1 class="text-center my-5">

{# Si la variable editMode retourne TRUE, cela veut dire que la catégorie possède un ID, alors c'est une modification #}
{% if editMode %}
   Modificaton Catégories
   
   {# Sinon la catégorie ne possède pas d'ID, c'est une insertion #}
   {% else %}
   Nouvelle catégorie
   {% endif %}
</h1>

{{ form_start(formCategory, ) }}

   {{ form_row(formCategory.titre) }}

   {{ form_row(formCategory.description, {'attr': {'class' : 'col-6 mx-auto'}}) }}

     <button class="btn btn-dark">Enregistrer</button>

{{ form_end(formCategory) }}

{% endblock %}


 {# </head>
    <body>
        <main class="d-flex">
            <div class="d-flex flex-column flex-shrink-0 p-3 text-white bg-dark position-fixed h-100" style="width: 20%;">
                <a href="{{ path('app_admin') }}" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-white text-decoration-none">
                <span class="fs-4"><i class="bi bi-bootstrap icone-logo"></i>ACKOFFICE</span>
                </a>
                <hr>
                <ul class="nav nav-pills flex-column mb-auto">
                <li class="nav-item">
                    <a href="{{ path('app_admin') }}" class="nav-link text-white" aria-current="page">
                    <i class="bi bi-house-door-fill"></i> Home
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('app_admin_articles_add') }}" class="nav-link text-white" aria-current="page">
                    <i class="bi bi-house-door-fill"></i> Nouvel article
                    </a>
                </li>

                <li>
                    <a href="{{ path('app_admin_articles') }}" class="nav-link text-white">
                    <i class="bi bi-book-fill"></i>
                    Articles
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_admin_categorie') }}" class="nav-link text-white">
                    <i class="bi bi-bookmarks-fill"></i>
                    Catégories
                    </a>
                </li>
                <li>
                    <a href="#" class="nav-link text-white">
                    <i class="bi bi-chat-left-fill"></i>
                    Commentaires
                    </a>
                </li>
                <li>
                    <a href="" class="nav-link text-white">
                    <i class="bi bi-people-fill"></i>
                    Users
                    </a>
                </li>
                </ul>
                <hr>
                <div class="dropdown">
                <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
                    <img src="https://github.com/mdo.png" alt="" width="32" height="32" class="rounded-circle me-2">
                    <strong>{{ app.user.prenom }} {{ app.user.nom }}</strong>
                </a>
                <ul class="dropdown-menu dropdown-menu-dark text-small shadow" aria-labelledby="dropdownUser1">
                    <li><a class="dropdown-item" href="{{ path('blog_create') }}">Nouvel article</a></li>
                    <li><a class="dropdown-item" href="{{ path('app_profil') }}">Votre compte</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="{{ path('blog') }}">Quitter</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="{{ path('app_logout') }}">Deconnexion</a></li>
                </ul>
                </div>
            </div>

            <div class="conteneur-principal-admin p-2">
            {% block body %}
                
            {% endblock %}
            </div>

        </main>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

        {% block javascripts %}
            
        {% endblock %}
    </body>
</html> #}